# Dockerfile.dev

FROM eclipse-temurin:17-jdk
WORKDIR /app

# 전체 프로젝트 복사
COPY . .

# Gradle 실행 권한 부여 (필요한 경우)
RUN chmod +x ./gradlew

# 빌드 - jar 이름 고정 필요 시 build.gradle에 설정
RUN ./gradlew bootJar

# 포트 노출
EXPOSE 8080

# jar 실행
CMD ["java", "-jar", "build/libs/app.jar"]
